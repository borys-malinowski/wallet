datasource db {
    provider = "sqlite"
    url      = env("CONNECTION")
}

generator client {
    provider = "cargo prisma"
    output   = "../prisma-cli/src/prisma.rs"
}

model User {
    id          String @id
    displayName String
}
